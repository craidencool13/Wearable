import { StyleSheet } from 'react-native';

/* ------ GLOBAL STYLE SHEET ------ */

export const GStyle = StyleSheet.create({
  mainContainer: {
    flex: 1,
  },
  headerTitle: {
    fontSize: 18,
    color: '#fff',
  },
});
